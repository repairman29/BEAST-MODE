# How to Run BEAST MODE CLI

## ✅ You're All Set!

The `beast-mode` command is now installed globally and can be run from **anywhere**!

## 🚀 Quick Start

### Run from Any Directory

```bash
# Just type this from anywhere:
beast-mode

# See all commands:
beast-mode --help

# Login with epic animation:
beast-mode login

# Check status:
beast-mode status
```

## 📍 Where to Run It

**You can run `beast-mode` from ANY directory now!**

```bash
# From your home directory
cd ~
beast-mode

# From any project
cd /path/to/any/project
beast-mode init

# From anywhere
beast-mode login
```

## 🎬 Try the Animations

```bash
# See animation on first run
beast-mode

# Login with epic animation
beast-mode login

# Direct animation commands
beast-mode artwork animate --kraken
beast-mode artwork animate --narwhal
beast-mode artwork animate --random
```

## 🔧 Installation Status

✅ **Installed globally via `npm link`**
- Command: `beast-mode`
- Location: `/opt/homebrew/bin/beast-mode`
- Works from: **Anywhere!**

## 📝 Common Commands

```bash
# First time setup
beast-mode              # See welcome animation
beast-mode login        # Login with animation

# Daily use
beast-mode status       # Check login status
beast-mode init         # Initialize in a project
beast-mode dashboard    # Launch dashboard

# Quality checks
beast-mode quality check
beast-mode quality score

# Artwork & animations
beast-mode artwork gallery
beast-mode artwork animate --kraken
```

## 🎯 Next Steps

1. **Try it now:**
   ```bash
   beast-mode
   ```

2. **Login:**
   ```bash
   beast-mode login
   ```

3. **Initialize a project:**
   ```bash
   cd /path/to/your/project
   beast-mode init
   ```

## 💡 Tips

- **First run**: Shows random creature animation if not logged in
- **Login**: Always shows epic animation
- **Enable animations**: `export BEAST_MODE_ANIMATE=true`
- **Quiet mode**: `beast-mode --quiet <command>`

---

**Ready to UNLEASH THE DEPTHS! 🦑⚔️🚀**

